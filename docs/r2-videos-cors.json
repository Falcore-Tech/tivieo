{
  "rules": [
    {
      "allowed": {
        "origins": ["http://localhost:3000", "https://tivieo.vercel.app"],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["content-type"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
